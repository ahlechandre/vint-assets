{
  "name": "vint-assets",
  "version": "0.5.0",
  "description": "vint frontend files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "NODE_ENV=production webpack",
    "prod-win": "set \"NODE_ENV=production\" && webpack",
    "prod-win-default": "set \"NODE_ENV=production\" && webpack --vint-default",
    "prod-win-dashboard": "set \"NODE_ENV=production\" && webpack --vint-dashboard",
    "prod-win-login": "set \"NODE_ENV=production\" && webpack --vint-login"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ahlechandre/vint-assets.git"
  },
  "author": "Alexandre Thebaldi",
  "license": "MIT",
  "private": true,
  "bugs": {
    "url": "https://github.com/ahlechandre/vint-assets/issues"
  },
  "homepage": "https://github.com/ahlechandre/vint-assets#readme",
  "devDependencies": {
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "jquery": "^2.2.4"
  }
}
